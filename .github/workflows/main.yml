name: Mon premier workflow
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    concurrency: waitforme
    permissions:
      contents: write

    steps:
      - run: |
          echo very long step
          sleep 1000
          each Done