name: Mon premier workflow
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    concurrency: 
      group: waitforme
      cancel-in-progress: true
    permissions:
      contents: write

    steps:
      - run: |
          echo very long step
          sleep 1000 !
          each Done