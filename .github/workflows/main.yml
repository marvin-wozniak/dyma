name: Mon premier workflow
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - run: |
          echo very long step
          sleep 100
          each Done