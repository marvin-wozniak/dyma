name: Mon premier workflow
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ubuntu, windows]
        include:
          - cloud: ovh
            endpoint: url1
            os: ubuntu
          
        
    steps:
      - run: echo ${{ matrix.cloud }} ${{ matrix.endpoint }} ${{ matrix.os }}
    
    